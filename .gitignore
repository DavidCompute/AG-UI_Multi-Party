bin/
obj/
*.user
.vs/
TestResults/
# 构建输出（发布目录 / MSI / WebView2 缓存 / 测试产物，勿入库）
artifacts/
# 本机桥/其它发布产物（如 dist/nativebridge 自包含单文件 exe，勿入库）
dist/
# 持久化状态文件（运行期生成，勿入库）
data/
# docker compose 环境变量（密钥等，勿入库）
.env
.env.*
!.env.example
# i18n 抽取报告（脚本可随时重新生成）
tools/extract-report.json
# node 依赖（如果有，勿入库）
node_modules/
# 本地大模型文件（运行时下载/生成，勿入库，超 GitHub 100MB 限制）
**/models/
*.gguf
