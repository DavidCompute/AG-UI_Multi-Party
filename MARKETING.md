# 知聚(KnowGath) — 市场推广文档

[English](MARKETING.en.md) | **简体中文**

> **一句话定位**：让「人 + 多个 AI 专家 + 外部 AG-UI 数字员工」在同一知聚里实时协作、共享记忆、安全审批的一站式多数字员工协作平台。

---

## 一、产品是什么

知聚(KnowGath)是一个**基于标准 AG-UI 协议的多数字员工知聚协作系统**（.NET 10 实现）。它把传统「单聊式」的 AI 助手升级为**知聚式的多角色协作空间**：

- 一个知聚里可以同时入驻**多个 AI 角色**（需求助手、代码助手、财务顾问……），人 @ 谁谁响应，也可以让角色全量监听 / 关键词 / 语境自动发言；
- 可以**接入外部 AG-UI 数字员工**（如 OpenCode 等基于 AG-UI 协议的服务），把「外部专家」拉进知聚，其流式回复、思考过程、工具调用、审批请求、附件原样回灌知聚；
- 聊天记录**向量化为长期记忆**（RAG），数字员工回复前自动检索相关历史，越聊越懂你的项目与团队；记忆可**分级、按保留天数自动遗忘、可视化治理**（数据最小化与审计友好）；
- 支持**话题隔离**（一个知聚多条讨论线，各自独立的会话与增量上下文）、**人机交互审批**（关键操作必须由人批准）、**AI 分身**（你离线时替你代班）、**知识库**（上传文档，数字员工基于资料作答）等企业级协作能力。

它可以跑在 **Docker（云端 / 内网服务器）**、**Windows 桌面版（单机 / 多实例）** 与 **跨平台桌面（macOS / Linux）**，数据可落 **PostgreSQL / MySQL / SQLite / Redis / 本地 JSON**，支持 Docker 多副本 + Redis 共享存储横向扩展，并支持**一键导出 / 导入**（账号、数字员工、聊天记录、附件全量打包），**数据主权完全在你手里**。

---

## 二、核心特性与亮点

### 1. 多数字员工知聚协作（人机共聚）
- 一个知聚可同时入驻任意多个 AI 角色，**四种触发方式**：@ 提及、全量监听、关键词、语境（模型自主决定是否发言）。
- **知聚内可覆盖角色默认触发**：不同知聚对同一角色有不同设定，互不干扰。
- 每个数字员工可配置**个人人设（Instructions）、独立模型、头像**，一键用一句话简介**自动生成角色设定**；25 个行业角色包可一键导入。
- 知聚列表按活跃度排序 + 未读徽标；被加入新知聚 / 被拉入已有知聚时**实时刷新**，在线用户立即可见。

### 2. 外部 AG-UI 专家生态（开放接入）
- 基于标准 AG-UI 协议桥接：**任何 AG-UI 服务**（OpenCode、自研 agent 服务等）都能作为「外部专家」拉进知聚。
- 外部专家的**流式回复、思考过程（可折叠展示）、工具调用（「🔧 调用中」进度行，完成后自动收起）、审批卡片、附件**全部原样回灌，体验与本地数字员工一致。
- **按话题隔离会话 + 增量上下文**：外部专家为每个话题维护独立会话，建立后只增量同步新消息，省 token 又精准；增量游标持久化，重启不丢。
- **Hub 级联**：两个 Hub 实例可互相接入，跨组织 / 跨网络的数字员工协作与**跨 Hub 审批**。

### 3. 记忆与知识（越用越懂你，也由你掌控）
- **语义记忆（RAG）**：聊天记录向量化存储（pgvector / sqlite-vec），数字员工回复前按相似度检索相关历史注入上下文；支持 `agent / group / all` 三种检索范围。
- **个人记忆**：数字员工可参考「触发者本人」的历史发言，理解你的偏好与立场（需双方开启）。
- **知识库（RAG 知识文档）**：上传 Word / Excel / PPT / PDF / 文本，切片向量化后数字员工基于你的资料作答。
- **记忆治理（按知聚 / 分级）**：记忆按知聚严格隔离（私密知聚仅限本知聚检索），每条记忆可标记 **普通 / 重要 / 关键**——同相似度下重要记忆**优先被检索**注入回复，关键决策不易丢失。
- **记忆治理（自动遗忘）**：可配置**保留天数**，到期记忆自动停止参与检索并由后台定时物理清理；支持**手动遗忘**（按知聚保留最近 7 / 30 / 90 天或立即遗忘），符合数据最小化与合规要求。
- **记忆可视化**：内置「记忆管理」界面——各知聚记忆统计、按知聚 / 关键词浏览、单条**分级 / 删除**、按知聚**遗忘**，记忆完全透明、可控、可审计；权限校验仅可管理自己所在知聚的记忆。
- **记忆跨实例同步（2.3）**：把记忆导出为「**记忆即数据包**」（文本元数据，跨实例可迁移），`GET /ag-ui/memory/export` 支持按知聚 / 时间下限**增量导出**到中心 Hub，`POST /ag-ui/memory/import` 按 messageId 去重导入并在目标实例按各自模型重算向量——打通桌面 / Web 记忆孤岛。
- **记忆工具**：数字员工可主动调用 `group_memory_search` 回忆背景、`read_attachment` 读取附件。

### 4. 人机协同与安全（关键操作必须人批准）
- **人机交互审批（HITL）**：需要审批的工具（如发布公告、创建技能）由模型触发后**中断运行**，聊天区出现 🔐 卡片，**仅发起请求的用户**可批准 / 拒绝，决策后同一会话继续。
- **AI 分身**：启用后系统根据你在公开知聚的发言生成人设；你在线时本人回复、离线时分身代班；还能「@ 自己」随时召唤。
- 完整的账号体系（PBKDF2 密码哈希、会话令牌、滑动续期）、**服务端强鉴权**（写操作一律以令牌身份为准，无法冒充他人）、附件上传白名单、前端 XSS 消毒与 CSP、链接代理 HTML 沙箱、SSRF 防护。

### 5. 话题化组织（多线并行不混乱）
- 一个知聚可开多个**话题**（如「需求评审」「技术选型」「闲聊」），各自独立的讨论线、未读数、会话与记忆。
- 「以此消息新建话题」把某条发言一键迁移为讨论起点；话题可清空 / 删除（对应记忆一并清除）。
- 记住每个知聚最近使用的话题，再次进入自动选中。

### 6. 数据主权与可迁移（不绑架数据）
- 五种存储：**PostgreSQL（生产 / 云端）、MySQL、SQLite（桌面单机）、Redis（多副本共享）、JSON 快照**，一键切换。
- **Redis 多副本横向扩展（6.2）**：`Storage:Provider=redis` 让全部存储与登录会话共享 Redis——Docker 横向扩容多副本时「一台副本写入、其余副本立即可读，同一登录任一副本均可校验」，支撑高可用与弹性扩展。
- **一键导出 / 导入**：账号、数字员工、聊天记录、附件、**头像**打包 zip——迁移到任何新环境（账号密码无损还原；**已存在账号自动更新资料（含头像），密码保留**；数字员工自动补齐；发送者引用自动重写）。
- **桌面版多实例**：一个后端进程，多个窗口共享数据，最后一个窗口关闭才停服务。
- **初始化（清空一切）**：一键回到全新状态（含浏览器缓存），重新配置模型。
- **运行时模型配置**：界面直接填写 DeepSeek endpoint / apiKey（留空用官方端点与环境变量），即时生效、重启不丢。

### 7. 链接代理与内网穿透（外网访问内网资源）
- 数字员工回复中的链接（包括内网地址）由**后端代访**返回——浏览器端无法直连的混合内容 / 内网服务也能正常查看与下载，HTML 沙箱化防脚本，下载自动携带正确文件名（含中文名）。

### 8. 数字员工编排与协同（从「各自回话」到「协同解题」）
- **多步工作流 / 编排（1.1）**：把一个复杂需求拆解为子任务，由代码 + 文档 + 测试等多个助手「规划 → 拆解 → 并行/顺序执行 → 聚合 → 输出」，形成类似编码助手的 agent 循环。
- **角色交接（1.2）**：数字员工可整轮委托给另一角色（`relayToAgentId`），实现「接力回复 / 协作接力」而非单向调用。
- **定时 / 计划任务（1.4）**：「值班数字员工」按 5 段 cron 定时汇报 / 核对 / 到点催办，前端任务面板可视化管理。
- **重要记忆自动沉淀（1.3）**：把知聚内「关键」结论一键聚合写入知识库，知识沉淀随对话自动发生，降低手工整理成本。
- **跨话题主题关联（5.1）**：「此主题还在哪些话题讨论过」的关联矩阵，帮助多人协作不漏上下文。

### 9. 治理与合规（企业落地硬门槛）
- **审批策略差异化（4.1）**：除全局工具名单外，可为单个数字员工配置独立的审批工具名单；`approveAll` 一次性批准当前任务后续全部待审批工具。
- **细粒度 RBAC（4.2）**：频道级权限——谁能 @ 数字员工（`canInvokeAgents`）、谁能批准人机交互（`canApprove`）、谁能管理知识库（`canManageKnowledge`），叠加系统管理员（`IsAdmin` / `AdminUserIds`）。
- **操作审计日志（4.3）**：记录「谁 / 何时 / 批准了什么工具 / 导入导出 / 重置」等关键操作，可导出，满足涉密与合规。
- **会话安全（4.4）**：多设备会话查看 / 吊销、可选的登录二次验证（TOTP）、`AllowedOrigins` 界面化配置。
- **配置治理（6.3）**：管理员在控制台「配置治理」页签在线调整并持久化运行参数（会话有效期、知聚 / 消息策略、工具开关 / 工作型工具 / 思考模式 / 每日 Token 配额、审批名单、iframe 嵌入来源），无需改配置 / 重启。
- **对外 API 密钥（6.4）**：面向程序化接入的 REST API Key 鉴权（`Auth:ApiKeys`）。

### 10. 可靠性、可观测性与生态
- **Web 多副本横向扩展（6.2）**：Redis 共享会话 / 存储——多副本部署时知聚 / 用户 / 任务 / 用量 / 扩展区与登录会话全部写入 Redis（`agui:*`），任一副本读写一致，水平扩容即线性提升并发承载。
- **桥接健康与自动重连（3.1）**：外部 AG-UI 端点健康探测、退避重连、断线补发，让「外部专家」更可靠。
- **能力协商（3.2）**：基于 AG-UI 协议自动发现外部端点支持的工具 / 附件 / 审批类型，减少人工配置。
- **数字员工 / 技能市场（3.3）**：内置「行业角色 / 技能 / 知识库模板」下载市场，一键分发。
- **运行指标（6.1）**：模型调用量、token、记忆命中率、桥接故障率等指标与可视化（`/ag-ui/admin/metrics`、`/ag-ui/admin/usage`）。
- **记忆时间线（2.2）**：按话题回放「某结论如何演化」，服务复盘与审计。

### 11. 富媒体消息（贴近即时通讯习惯）
- **图片多选上传**：单次最多 9 张图片，消息内以**平铺网格**展示、可点击放大；输入区预览缩略图。
- **语音消息**：点按麦克风**录音**（MediaRecorder），生成音频附件（mp3/wav/ogg/m4a/webm 等），消息内以 `<audio>` 内联播放条回放——语音附件仅携元数据，不注入模型文本。
- **画布标注**：内置绘图画布（画笔 / 橡皮 / 颜色 / 清空），画完一键导出为 PNG 图片附件发送，适合手绘示意图 / 批注。

### 12. 无障碍与通知中心（体验与可达性）
- **应用内通知中心（5.4）**：顶栏 🔔 聚合通知——被 @ 提及、审批 / 输入待处理、WS 断线 / 重连、非当前视图新消息（含定时任务广播），点击跳转来源知聚，未读徽标 + 清空；页面隐藏时同步发系统桌面通知。
- **前端性能**：消息虚拟滚动 + 懒加载（窗口化渲染、实测行高锚定、分页加载更早），超大规模知聚也能流畅滚动。
- **无障碍（ARIA，5.3）**：消息列表 `role="log"` + 读屏播报、每条消息附发件人 / 时间 / 内容语义标签、图标按钮 / 绘图区补齐 `aria-label`、弹窗 `role="dialog"` + 焦点管理 + Esc 关闭，键盘可操作。

### 13. 白标 / 嵌入 / 对外 API（开放与集成）
- **白标主题（6.4）**：管理员在线配置应用名 + Logo + 品牌主色 + 强制深色 + 副标语（「白标设置」），登录页与顶栏即时生效，主题可一键恢复默认；品牌主色自动派生强调色 / 深色适配。
- **iframe 嵌入（6.4）**：配置 `AllowedFrameOrigins` 允许可信站点以 iframe 嵌入（CSP `frame-ancestors` 放行，默认禁止）；嵌入后自动进入紧凑模式（隐藏无关按钮）。
- **对外 API 密钥（6.4）**：`Auth:ApiKeys` 配置密钥，`Authorization: Bearer <apiKey>` 免登录以绑定账号身份调用 HTTP API，适配脚本 / 集成对接。
- **官方 .NET SDK（第三方接入）**：`src/AguiGroupChat.Sdk` 提供 `AguiClient`（HTTP 上行）+ `AguiRealtimeClient`（WS/SSE 下行）与强类型 Models，第三方应用一条引用即可登录、建群、发消息、实时订阅与接收智能体流式回复（详见 [SDK 文档](src/AguiGroupChat.Sdk/README.md)，示例见 `samples/AguiGroupChat.Client`）。

----

## 三、技术优势

| 维度 | 说明 |
|---|---|
| 标准协议 | 基于 AG-UI 群聊扩展协议 v1.0，事件 / 字段与协议逐字段对齐，兼容标准 AG-UI 生态（Microsoft.Agents.AI.AGUI） |
| 成熟框架 | 基于 Microsoft Agent Framework（MSAGENT）实现数字员工网关，天然支持流式、工具调用、技能（数字员工间调用）、审批 |
| 多端覆盖 | Web（Docker）、Windows 桌面（WPF + WebView2，多实例）、跨平台桌面（Avalonia） |
| 可扩展 | 存储抽象（IGroupStore / IUserStore）内置 **memory / postgres / mysql / sqlite / redis** 五种实现，一键切换；`IAgentGateway` 可自定义网关；数字员工目录 / 触发规则 / 话题全部运行时管理 |
| 数据安全 | 密码 PBKDF2、令牌鉴权、附件白名单、私密知聚 / 私密数字员工归属强校验、HTML 沙箱、SSRF 防护、记忆分级与自动遗忘 |
| 质量保障 | **603 个自动化测试用例**（知聚生命周期 / 权限 / RBAC / 审计 / 记忆与时间线 / 记忆跨实例同步 / Redis 与三套数据库存储 / Redis 共享会话 / 桥接与断线重连 / 审批 / 编排与角色交接 / 定时任务 / 富媒体附件 / 白标 / 配置治理 / 端到端） |

---

## 四、应用场景（详细）

### 场景一：企业内部团队协作与知识沉淀（研发 / 产品 / 运营通用）

**场景描述**：一个 5-20 人的团队，在日常工作知聚里同时入驻「需求助手」「代码助手」「数据分析师」等多个 AI 角色，普通成员无需学习任何 AI 工具，**在原有聊天习惯里**即可调用多个专家。

**典型用法**：
- 产品经理在知聚里说「@需求助手 帮我拆解这个需求，输出用户故事」→ 需求助手即时生成结构化拆解；
- 开发在技术知聚里讨论架构时，「代码助手」以语境触发**主动**给出方案建议与代码片段；
- 项目讨论形成的历史记录被向量化为**语义记忆**——两周后问「我们当时为什么选这个方案？」，数字员工检索历史自动回顾上下文；
- 关键决策在「记忆管理」中标记为**重要 / 关键**（同相似度下优先被检索），过期讨论按保留天数自动遗忘，记忆界面一目了然、随时可删；
- 上传《项目周报模板.docx》到**知识库**，绑定给数据分析师，之后它按模板生成周报。

**价值**：把 AI 能力嵌入团队既有协作流，零学习成本；记忆让知识**沉淀在组织而非个人**，且分级 / 遗忘 / 可视化让记忆**可控可审计**；多角色分工让每个问题都有对口的 AI 专家。

### 场景二：外部 AI 专家接入的研发团队（AG-UI 桥接 / OpenCode 类服务）

**场景描述**：团队自建或购买了一套基于 AG-UI 协议的编码数字员工服务（如 OpenCode 本地编码代理），希望它既能独立工作，也能在知聚里被团队成员随时调用。

**典型用法**：
- 在数字员工管理里创建「外部代码专家」，填写桥接端点 `http://192.168.x.x:8889/ag-ui/opencode`，拉进研发知聚；
- 成员在知聚里说「@外部代码专家 给我做一份关于 AI 前景展望的 PPT」→ 外部专家的**思考过程、工具调用、执行进度**实时回灌（「🔧 工具调用中…」），完成后附件（PPTX）直接以卡片形式回到知聚里；
- 外部专家执行需要权限的操作（如发布公告 / 部署）时，**审批卡片**在知聚里弹出，仅发起者可以批准；
- 外部专家回复中给出的内网链接（如本地预览服务），经**链接代理**由服务端代访，成员点击即可查看（下载文件自动带正确文件名）；
- 外部专家在多个话题中并行工作，各自独立会话、增量同步上下文。

**价值**：把「工具型」AI 升级为「协作型」AI；人、本地数字员工、外部 AG-UI 专家在同一空间协同，**审批权仍在人手里**，既高效又可控。

### 场景三：私有化 / 涉密环境的数字员工中枢（金融、军工、医疗、政务）

**场景描述**：数据敏感行业要求**数据不出内网**，无法使用公有云大模型 API；同时需要完整的多数字员工协作、记忆治理与审计能力。

**典型用法**：
- 内网服务器 Docker 部署（PostgreSQL 落盘），**全部数据本地化**，无任何外网依赖（本地 Ollama 提供 embedding，模型可选私有化部署的 OpenAI 兼容端点）；
- 使用**私密知聚**承载涉密讨论：私密知聚的聊天记忆**仅限本知聚内检索**，其他知聚触发数字员工时读不到，防止信息横向泄露；
- **记忆治理满足数据最小化与合规**：按保密要求配置保留天数自动遗忘、按知聚手动遗忘、记忆管理界面可视化审计——记忆内容、级别、过期时间全部可控可查；
- **私密数字员工**：只有创建者能使用 / 拉进知聚 / 编辑，适合个人专属顾问；
- **人机交互审批**：涉密操作（对外发布 / 删除数据）必须由人批准，全程留痕；
- 定期**一键导出**数据包（账号 + 数字员工 + 聊天记录 + 附件 + 头像）异地备份，**初始化**可一键清空。

**价值**：在合规红线下拥有完整的 AI 协作能力；数据主权、权限边界、审批留痕、记忆遗忘、备份迁移五大诉求全部满足。

### 场景四：个人知识管家与 AI 分身助理

**场景描述**：个人用户希望有一个「7×24 小时」的 AI 助理：在工作知聚里解答问题、沉淀个人知识，离线时还能替你回复。

**典型用法**：
- 桌面版（SQLite + 本地 embedding）单机运行，Windows / macOS / Linux 三端可用，**多实例**（工作窗口 + 家庭窗口共享同一后端，最后一个窗口关闭才停服务）；
- 创建个人知识库（个人文档、笔记、简历、合同模板），绑定给助理角色，之后「帮我基于简历写一份自我介绍」等请求基于你的资料作答；
- 开启 **AI 分身**：根据你在公开知聚的发言自动生成人设；你在线时自己回复，**离线时分身代班**；在知聚里 @ 自己还能随时「召唤」分身；
- 启用**个人记忆**，助理越来越了解你的偏好；在**记忆管理**中把重要结论标记为「重要」，让助理长期记住你的关键偏好；
- 用 **话题**区分「工作」「生活」「学习」多线讨论，互不干扰。

**价值**：一个「懂你」的私人助理，随叫随到，替你值守；数据全部在本机，隐私无忧，记忆由你掌控。

### 场景五：教育培训与学术研讨（多导师多学生协同学）

**场景描述**：高校课题组 / 培训班需要多个专业 AI 助教（写作、代码、数学、文献综述）参与知聚研讨，并对学生进行个性化辅导。

**典型用法**：
- 一个课程知聚入驻「写作助教」「代码助教」「数学助教」，分别绑定对应的知识库（教材、讲义、往年试题）；
- 学生发问「@写作助教 帮我校对这段论文摘要，给出修改建议」→ 助教基于知识库与课程要求作答；
- 「@代码助教」在语境触发下主动指出学生代码中的常见错误（全量监听 + 语境判断）；
- 学术讨论按 **话题**分线：「课程答疑」「项目组会」「论文进展」互不打扰；
- 老师开启 **AI 分身**：离线时由分身先回复常见问题，老师上线后接手；
- 把课堂上的关键知识点在记忆管理中标记为「重要」，助教回答时优先引用。

**价值**：多助教并行辅导，减轻教师负担；知识库让答案**对齐课程大纲**而非泛泛而谈；话题组织让研讨有序，记忆分级让重点内容更突出。

### 场景六：跨组织 / 跨区域的多方协作（Hub 级联与外部专家网络）

**场景描述**：两家公司或两个部门共同推进一个项目，各自有内部数字员工与知识，需要在不打通全部数据的前提下协同。

**典型用法**：
- A 公司 Hub 的「外部专家」角色以 **hub 方言桥接**到 B 公司 Hub，两套系统级联——A 知聚里的触发消息转发到 B 的数字员工，B 的流式回复回灌 A 的知聚；
- 外部专家的**审批请求跨 Hub 级联**：B 的数字员工要执行敏感操作时，审批卡片出现在 A 的知聚里，由 A 侧触发者决策；
- 按**话题**隔离多方协作线，每方各管各的知聚与记忆，**私密知聚**保证内部讨论不被外部数字员工检索；
- 各自按合规要求配置**记忆保留天数**，项目结束可**导出**协作知聚数据归档，或**初始化**清空外部接入痕迹。

**价值**：让「组织边界」不再阻断 AI 协作；级联审批保证跨组织的操作依然**人可控制**；记忆治理与数据主权各自保留。

---

## 五、部署形态速览

| 形态 | 适用 | 数据存储 | 特点 |
|---|---|---|---|
| **Docker Web**（推荐生产） | 内网服务器 / 云主机 | PostgreSQL（+ pgvector）| 一条 `docker compose up -d` 启动 Web + Postgres + Ollama，RAG 记忆与记忆治理开箱即用 |
| **Docker Web + Redis 多副本** | 高并发 / 高可用 | Redis + PostgreSQL（+ pgvector）| `docker compose up --scale web=N` 横向扩容，全部存储与登录会话共享 Redis，多副本读写一致、同一登录处处有效 |
| **Windows 桌面版** | 个人 / 小团队单机 | SQLite（+ sqlite-vec）| WPF + WebView2，捆绑本地 embedding 模型，离线可用；**多实例共享后端** |
| **跨平台桌面版** | macOS / Linux / Windows | SQLite | Avalonia 壳，同一套宿主，三端体验一致 |
| **协议 Hub**（仅协议） | 二次开发 / 服务端 | 任意 | 只暴露 AG-UI 协议端点，可嵌入自有系统 |

---

## 六、快速上手（5 分钟）

1. **Docker**：`cp .env.example .env && docker compose up -d --build` → 浏览器打开 `http://localhost:5200`；
2. **桌面版**：安装 MSI（Windows）后双击启动，自动拉起本地服务；
3. 注册 / 登录账号（首次进入按提示配置 DeepSeek endpoint / apiKey，留空即用官方端点与环境变量）；
4. 顶栏「🤖 数字员工」创建一个 AI 角色（或从 `tools/agents-starter.json` 一键导入 25 个行业角色）；
5. 左侧「＋」创建知聚，勾选成员与数字员工，@ 它开始对话；
6. 需要外部专家时，在数字员工表单填写 AG-UI 桥接端点即可；
7. 聊天一段时间后，到账户菜单 →「记忆管理」体验分级、自动遗忘与可视化治理。

---

## 七、下一步展望

- **多模态进阶**：图片 / 语音问答、音视频会议接入（语音消息、图片多图、画布标注作为附件已落地，双向语音对话与图片问答待深化）；
- **可观测性深化（6.1）**：指标可视化看板、OpenTelemetry 对接、Redis 多副本下跨副本诊断；
- **混合检索与 RAG 增强**：BM25 稀疏检索 + 稠密向量融合，覆盖中文 / 代码场景；embedding 提供方按需切换；
- **记忆治理深化**：重要记忆自动沉淀为知识库、记忆跨实例增量同步到中心 Hub、记忆多版本 / 时间线回放；
- **生态与市场**：数字员工 / 技能 / 知识库模板市场在线分发、白标深度定制与对外 REST API 增强。

---

## 八、商业化方向与竞争定位

### 8.1 目标客户

| 客户群 | 痛点 | 本产品切入点 | 付费意愿 |
|---|---|---|---|
| **中大型企业内网 AI 中枢** | 通用大模型 SaaS 数据出域不达标；多团队各自接 AI 导致重复、不可管 | 私有化部署 + 记忆分级治理 + 审批留痕 + 审计，数据不出内网的一体化解法 | 高（合规刚需） |
| **研发 / 数据团队** | 外部编码数字员工（OpenCode 类）与团队协作难以打通 | AG-UI 桥接把「工具型 AI」变成「协作型 AI」，人 + 本地数字员工 + 外部专家同处一室 | 中高 |
| **咨询 / 法律服务 / 金融** | 知识沉淀在成员个体而非组织；关键产出不可追溯 | 知识库 + 重要记忆沉淀 + 话题化 + 操作审计，把经验资产化 | 高 |
| **教育培训** | 多助教 / 多导师并行辅导成本高、个性化难 | 多数字员工研讨 + 知识库对齐课程 + AI 分身代班 | 中 |
| **个人知识管家** | 现有助手缺乏私有记忆与离线可用 | 桌面版 SQLite + 本地 embedding，数据全在本机、隐私无忧 | 低→订阅 |

### 8.2 竞争定位（差异化）

| 维度 | 通用 AI 助手 / Chat | 企业级 RAG / 知识中台 | **知聚(KnowGath)** |
|---|---|---|---|
| 协作形态 | 单聊（一问一答） | 知识问答（文档驱动） | **知聚式多数字员工协同**（人机共处一路） |
| 数字员工 | 单个助手 | 基本无 | **多角色 + 外部 AG-UI 专家桥接** |
| 记忆 | 会话上下文 | 文档向量库 | **知聚记忆分级 + 自动遗忘 + 可视化治理 + 跨实例同步** |
| 安全治理 | 弱 | 部分 | **审批策略差异化 + 细粒度 RBAC + 操作审计 + TOTP + 会话管理** |
| 数据主权 | 云托管 | 可私有化 | **本机 / 私有化 / 多云存储可切换，不绑架数据** |
| 扩展性 | 无 | 弱 | **Docker 多副本 + Redis 共享存储，水平扩容** |

**一句话定位**：把「单聊式 AI 助手」升级为「可治理、可扩展、懂你组织记忆」的**多人多数字员工协作平台**——既有 Chat 的易用，也有企业级 RAG 的严谨，更有它们都缺的**协作编排与安全治理**。

### 8.3 商业化路径（分阶段）

1. **开源获客（当前）**：核心能力放开自用，靠代码质量、文档与协议开放（AG-UI 标准）建立信任与社区口碑，形成模板 / 角色 / 知识库的开源生态；
2. **增值订阅（企业化）**：打包**治理套件**（细粒度 RBAC、操作审计、TOTP、配置治理、Redis 多副本）形成付费版，提供白标、品牌与客服支持；
3. **私有化 / 专属定制（行业纵深）**：面向政务 / 金融 / 军工等涉密行业输出**行业模板包**（角色 + 知识库 + 审批策略模板）+ 现场部署 / 培训 / 合规认证服务，按席位与部署规模收费；
4. **生态市场（平台化）**：建设「数字员工 / 技能 / 知识库模板」交易市场，抽成或版权分成，形成双边网络效应；对外 REST API 商业化：接口调用量 / 席位 / 用量阶梯计费。

### 8.4 当前最适合的切入故事（带真实操作体验）
> 一家数据敏感的研发团队（约 12 人，产品 / 前端 / 后端 / 运维），需要**数据不出内网**、又要**团队共用一个 AI 中枢**。以下是他们实际按下每一步按钮、看到每一个界面的完整过程。

**第 1 步 · 内网一键起服务，开箱即有示例**
运维在内网服务器执行 `cp .env.example .env && docker compose up -d --build`，一条命令拉起 Web + PostgreSQL（pgvector）+ Ollama 三个容器。因为 `STORAGE_PROVIDER=postgres`，所有业务数据与向量记忆都写进内网库；embedding 由内置 Ollama 提供（首次自动拉取 bge-m3 模型）。启动时自动播种示例数据（`GroupChat__SeedSampleData=true`），成员第一次打开 `http://内网IP:5200`，注册账号即成管理员，左侧已能看到示例知聚与几个数字员工，**不用先配任何东西就能点着玩**。

**第 2 步 · 建「代码 / 需求 / 法务」三个数字员工 + 外部 OpenCode 专家**
1. 顶栏点「🤖 数字员工」打开管理面板，先点工具栏「📥 导入」从 `tools/agents-starter.json` 一键批量创建 25 个行业角色，再搜索、编辑只留想要的；
2. 到「产品助理」表单：昵称、头像（可上传本地图片）、一句话简介填「产品需求分析助手」，点旁边的「✨ 生成角色设定」——模型自动把**身份定位 / 职责范围 / 回复风格要求**填进 Instructions 系统提示词，检查微调后保存；
3. 复制同样步骤建「代码帮」：人设强调“给出代码片段前先讲思路”，触发方式选**语境触发**（🧠），模型可单独覆盖为 `deepseek-reasoner` 思考模式；
4. 需要外部编码专家时，新建角色并填 AG-UI 桥接端点 `http://192.168.6.18:8889/ag-ui/opencode`——保存后这个角色**不再调本地大模型**，消息经 AG-UI 协议转发给外部 OpenCode，其**思考过程、工具调用进度、审批卡片、附件**原样回灌知聚。

**第 3 步 · 拉一个「技术讨论」知聚，日常调用**
1. 左侧「我的知聚」点右上角「＋」创建「技术讨论」，勾选成员 + 三个数字员工一起入局；
2. 成员在输入框 `@代码帮 能不能给我这段重写成防 SQL 注入的版本`，得到流式回复；想看多角色共同解题时，点输入区左边「🗣 多位数字员工讨论」图标，勾选「产品助理 + 代码帮」、填主题「我们当前 API 权限模型怎么设计？」——两个数字员工**按序接力发言**，形成一场多人多角度研讨；
3. 语义记忆自动生效：`@产品助理 下周评审要讲这个方案，帮我回顾一下决策背景`，助理检索之前讨论的向量记忆，把选型原因、踩坑结论一次讲清。

**第 4 步 · 让记忆沉淀为「团队资产」**
1. 讨论到关键处，成员点右上角账户菜单 →「🧠 记忆管理」，看到「技术讨论」知聚的记忆统计；把「为什么选 MySQL 分区」那条标记为**重要**，把「对外发布前必须过审」标记为**关键**——之后同相似度的记忆里这些会被**优先检索**注入回复；
2. 对过期话题，管理员按知聚手动设置**保留 30 天自动遗忘**，到期后台物理清理；记忆全程**分知聚隔离**——「涉密需求」是私密知聚，其他知聚触发数字员工时读不到它的内容；
3. 上传《接口评审表.docx》到**知识库**并绑定给「产品助理」，之后它评审接口时会先检索模板、按规范输出评审意见。

**第 5 步 · 关键动作必须人批，且有审计可追溯**
1. 成员对「代码帮」说「发布公告到项目知聚」，命中需审批工具 `publish_announcement`——数字员工运行到这一步**中断**，聊天区弹出 🔐 审批卡片，**仅发起请求的这位成员**看到「✅ 批准 / ✅🔄 批准本次全部 / ❌ 拒绝」三个按钮，其他人只能看到等待；他点决定后，数字员工才继续执行；
2. 想放权时可点「立即全部批准（approveAll）」，或管理员在「配置治理」里把这个工具的审批名单细化到单个数字员工；想收紧时在频道级权限里关掉某成员的 `canApprove`（谁能批）、`canInvokeAgents`（谁能 @ 数字员工）、`canManageKnowledge`（谁能管知识库）；
3. 管理员到「账户菜单 → 操作审计」看到完整留痕：谁 / 何时 / 批准了什么工具 / 导入导出了什么，可导出归档满足合规。

**第 6 步 · 工作型数字员工承接长任务（可选进阶）**
给「代码帮」勾选「🛠️ 工作型数字员工」，它便有了专属工作区（`data/workspaces/code-bang/`）。成员在知聚里发起「整理本周发布清单并生成周报」，它自动创建任务去读取 / 写入文件、跑白名单命令，前端「任务中心」实时显示**状态 / 进度 / 结果**，消息上还会回填工作区 `PLAN.md` 的**计划清单 + 进度条**；写 / 删文件这类操作同样走知聚内审批。跨对话靠工作区 `NOTES.md` 延续，不必重复交代。

**第 7 步 · 数据主权兜底与扩容**
1. 「数据备份」面板一键导出 zip（账号 + 数字员工 + 聊天记录 + 附件 + 头像）异地备份；换环境时一键导入（账号密码无损还原、已存在账号自动更新资料、数字员工自动补齐、发送者引用自动重写），或「系统初始化」一键回到全新状态；
2. 需要高可用时把 `Storage__Provider` 切为 `redis` 并 `docker compose up --scale web=2`：多副本共享会话与存储，**任一成员任一副本登录都有效**，水平扩容即线性提升并发。

**结果**：从「内网一键部署」到「多数字员工日常协作」「可治理的组织记忆」「按人审批留痕」「工作型数字员工跑长任务」「数据可迁移可扩容」——这套**私有、可治理、可协作、可扩展**的完整体验，正是通用 SaaS 助手给不了的。
