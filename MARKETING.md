# AG-UI 群聊扩展平台 — 市场推广文档

> **一句话定位**：让「人 + 多个 AI 专家 + 外部 AG-UI 智能体」在同一群聊里实时协作、共享记忆、安全审批的一站式多智能体协作平台。

---

## 一、产品是什么

AG-UI 群聊扩展平台是一个**基于标准 AG-UI 协议的多智能体群聊协作系统**（.NET 10 实现）。它把传统「单聊式」的 AI 助手升级为**群聊式的多角色协作空间**：

- 一个群里可以同时入驻**多个 AI 角色**（需求助手、代码助手、财务顾问……），人 @ 谁谁响应，也可以让角色全量监听 / 关键词 / 语境自动发言；
- 可以**接入外部 AG-UI 智能体**（如 OpenCode 等基于 AG-UI 协议的服务），把「外部专家」拉进群，其流式回复、思考过程、工具调用、审批请求、附件原样回灌群聊；
- 聊天记录**向量化为长期记忆**（RAG），智能体回复前自动检索相关历史，越聊越懂你的项目与团队；记忆可**分级、按保留天数自动遗忘、可视化治理**（数据最小化与审计友好）；
- 支持**话题隔离**（一个群多条讨论线，各自独立的会话与增量上下文）、**人机交互审批**（关键操作必须由人批准）、**AI 分身**（你离线时替你代班）、**知识库**（上传文档，智能体基于资料作答）等企业级协作能力。

它可以跑在 **Docker（云端 / 内网服务器）**、**Windows 桌面版（单机 / 多实例）** 与 **跨平台桌面（macOS / Linux）**，数据可落 **PostgreSQL / MySQL / SQLite / 本地 JSON**，并支持**一键导出 / 导入**（账号、智能体、聊天记录、附件全量打包），**数据主权完全在你手里**。

---

## 二、核心特性与亮点

### 1. 多智能体群聊协作（人机共群）
- 一个群可同时入驻任意多个 AI 角色，**四种触发方式**：@ 提及、全量监听、关键词、语境（模型自主决定是否发言）。
- **群内可覆盖角色默认触发**：不同群对同一角色有不同设定，互不干扰。
- 每个智能体可配置**个人人设（Instructions）、独立模型、头像**，一键用一句话简介**自动生成角色设定**；25 个行业角色包可一键导入。
- 群列表按活跃度排序 + 未读徽标；被加入新群 / 被拉入已有群时**实时刷新**，在线用户立即可见。

### 2. 外部 AG-UI 专家生态（开放接入）
- 基于标准 AG-UI 协议桥接：**任何 AG-UI 服务**（OpenCode、自研 agent 服务等）都能作为「外部专家」拉进群。
- 外部专家的**流式回复、思考过程（可折叠展示）、工具调用（「🔧 调用中」进度行，完成后自动收起）、审批卡片、附件**全部原样回灌，体验与本地智能体一致。
- **按话题隔离会话 + 增量上下文**：外部专家为每个话题维护独立会话，建立后只增量同步新消息，省 token 又精准；增量游标持久化，重启不丢。
- **Hub 级联**：两个 Hub 实例可互相接入，跨组织 / 跨网络的智能体协作与**跨 Hub 审批**。

### 3. 记忆与知识（越用越懂你，也由你掌控）
- **语义记忆（RAG）**：聊天记录向量化存储（pgvector / sqlite-vec），智能体回复前按相似度检索相关历史注入上下文；支持 `agent / group / all` 三种检索范围。
- **个人记忆**：智能体可参考「触发者本人」的历史发言，理解你的偏好与立场（需双方开启）。
- **知识库（RAG 知识文档）**：上传 Word / Excel / PPT / PDF / 文本，切片向量化后智能体基于你的资料作答。
- **记忆治理（分群分级）**：记忆按群严格隔离（私密群仅限本群检索），每条记忆可标记 **普通 / 重要 / 关键**——同相似度下重要记忆**优先被检索**注入回复，关键决策不易丢失。
- **记忆治理（自动遗忘）**：可配置**保留天数**，到期记忆自动停止参与检索并由后台定时物理清理；支持**手动遗忘**（按群保留最近 7 / 30 / 90 天或立即遗忘），符合数据最小化与合规要求。
- **记忆可视化**：内置「记忆管理」界面——各群记忆统计、按群 / 关键词浏览、单条**分级 / 删除**、按群**遗忘**，记忆完全透明、可控、可审计；权限校验仅可管理自己所在群的记忆。
- **记忆工具**：智能体可主动调用 `group_memory_search` 回忆背景、`read_attachment` 读取附件。

### 4. 人机协同与安全（关键操作必须人批准）
- **人机交互审批（HITL）**：需要审批的工具（如发布公告、创建技能）由模型触发后**中断运行**，聊天区出现 🔐 卡片，**仅发起请求的用户**可批准 / 拒绝，决策后同一会话继续。
- **AI 分身**：启用后系统根据你在公开群的发言生成人设；你在线时本人回复、离线时分身代班；还能「@ 自己」随时召唤。
- 完整的账号体系（PBKDF2 密码哈希、会话令牌、滑动续期）、**服务端强鉴权**（写操作一律以令牌身份为准，无法冒充他人）、附件上传白名单、前端 XSS 消毒与 CSP、链接代理 HTML 沙箱、SSRF 防护。

### 5. 话题化组织（多线并行不混乱）
- 一个群可开多个**话题**（如「需求评审」「技术选型」「闲聊」），各自独立的讨论线、未读数、会话与记忆。
- 「以此消息新建话题」把某条发言一键迁移为讨论起点；话题可清空 / 删除（对应记忆一并清除）。
- 记住每个群最近使用的话题，再次进入自动选中。

### 6. 数据主权与可迁移（不绑架数据）
- 四种存储：**PostgreSQL（生产 / 云端）、MySQL、SQLite（桌面单机）、JSON 快照**，一键切换。
- **一键导出 / 导入**：账号、智能体、聊天记录、附件、**头像**打包 zip——迁移到任何新环境（账号密码无损还原；**已存在账号自动更新资料（含头像），密码保留**；智能体自动补齐；发送者引用自动重写）。
- **桌面版多实例**：一个后端进程，多个窗口共享数据，最后一个窗口关闭才停服务。
- **初始化（清空一切）**：一键回到全新状态（含浏览器缓存），重新配置模型。
- **运行时模型配置**：界面直接填写 DeepSeek endpoint / apiKey（留空用官方端点与环境变量），即时生效、重启不丢。

### 7. 链接代理与内网穿透（外网访问内网资源）
- 智能体回复中的链接（包括内网地址）由**后端代访**返回——浏览器端无法直连的混合内容 / 内网服务也能正常查看与下载，HTML 沙箱化防脚本，下载自动携带正确文件名（含中文名）。

---

## 三、技术优势

| 维度 | 说明 |
|---|---|
| 标准协议 | 基于 AG-UI 群聊扩展协议 v1.0，事件 / 字段与协议逐字段对齐，兼容标准 AG-UI 生态（Microsoft.Agents.AI.AGUI） |
| 成熟框架 | 基于 Microsoft Agent Framework（MSAGENT）实现智能体网关，天然支持流式、工具调用、技能（智能体间调用）、审批 |
| 多端覆盖 | Web（Docker）、Windows 桌面（WPF + WebView2，多实例）、跨平台桌面（Avalonia） |
| 可扩展 | 存储抽象（IGroupStore / IUserStore）可替换 Redis / 数据库；`IAgentGateway` 可自定义网关；智能体目录 / 触发规则 / 话题全部运行时管理 |
| 数据安全 | 密码 PBKDF2、令牌鉴权、附件白名单、私密群 / 私密智能体归属强校验、HTML 沙箱、SSRF 防护、记忆分群分级与自动遗忘 |
| 质量保障 | 498 个自动化测试用例（群生命周期 / 权限 / 可见性 / 记忆 / 三套数据库 / 桥接 / 审批 / 端到端） |

---

## 四、应用场景（详细）

### 场景一：企业内部团队协作与知识沉淀（研发 / 产品 / 运营通用）

**场景描述**：一个 5-20 人的团队，在日常工作群里同时入驻「需求助手」「代码助手」「数据分析师」等多个 AI 角色，普通成员无需学习任何 AI 工具，**在原有聊天习惯里**即可调用多个专家。

**典型用法**：
- 产品经理在群里说「@需求助手 帮我拆解这个需求，输出用户故事」→ 需求助手即时生成结构化拆解；
- 开发在技术群里讨论架构时，「代码助手」以语境触发**主动**给出方案建议与代码片段；
- 项目讨论形成的历史记录被向量化为**语义记忆**——两周后问「我们当时为什么选这个方案？」，智能体检索历史自动回顾上下文；
- 关键决策在「记忆管理」中标记为**重要 / 关键**（同相似度下优先被检索），过期讨论按保留天数自动遗忘，记忆界面一目了然、随时可删；
- 上传《项目周报模板.docx》到**知识库**，绑定给数据分析师，之后它按模板生成周报。

**价值**：把 AI 能力嵌入团队既有协作流，零学习成本；记忆让知识**沉淀在组织而非个人**，且分级 / 遗忘 / 可视化让记忆**可控可审计**；多角色分工让每个问题都有对口的 AI 专家。

### 场景二：外部 AI 专家接入的研发团队（AG-UI 桥接 / OpenCode 类服务）

**场景描述**：团队自建或购买了一套基于 AG-UI 协议的编码智能体服务（如 OpenCode 本地编码代理），希望它既能独立工作，也能在群里被团队成员随时调用。

**典型用法**：
- 在智能体管理里创建「外部代码专家」，填写桥接端点 `http://192.168.x.x:8889/ag-ui/opencode`，拉进研发群；
- 成员在群里说「@外部代码专家 给我做一份关于 AI 前景展望的 PPT」→ 外部专家的**思考过程、工具调用、执行进度**实时回灌（「🔧 工具调用中…」），完成后附件（PPTX）直接以卡片形式回到群里；
- 外部专家执行需要权限的操作（如发布公告 / 部署）时，**审批卡片**在群里弹出，仅发起者可以批准；
- 外部专家回复中给出的内网链接（如本地预览服务），经**链接代理**由服务端代访，成员点击即可查看（下载文件自动带正确文件名）；
- 外部专家在多个话题中并行工作，各自独立会话、增量同步上下文。

**价值**：把「工具型」AI 升级为「协作型」AI；人、本地智能体、外部 AG-UI 专家在同一空间协同，**审批权仍在人手里**，既高效又可控。

### 场景三：私有化 / 涉密环境的多智能体中枢（金融、军工、医疗、政务）

**场景描述**：数据敏感行业要求**数据不出内网**，无法使用公有云大模型 API；同时需要完整的多智能体协作、记忆治理与审计能力。

**典型用法**：
- 内网服务器 Docker 部署（PostgreSQL 落盘），**全部数据本地化**，无任何外网依赖（本地 Ollama 提供 embedding，模型可选私有化部署的 OpenAI 兼容端点）；
- 使用**私密群**承载涉密讨论：私密群的聊天记忆**仅限本群内检索**，其他群触发智能体时读不到，防止信息横向泄露；
- **记忆治理满足数据最小化与合规**：按保密要求配置保留天数自动遗忘、按群手动遗忘、记忆管理界面可视化审计——记忆内容、级别、过期时间全部可控可查；
- **私密智能体**：只有创建者能使用 / 拉群 / 编辑，适合个人专属顾问；
- **人机交互审批**：涉密操作（对外发布 / 删除数据）必须由人批准，全程留痕；
- 定期**一键导出**数据包（账号 + 智能体 + 聊天记录 + 附件 + 头像）异地备份，**初始化**可一键清空。

**价值**：在合规红线下拥有完整的 AI 协作能力；数据主权、权限边界、审批留痕、记忆遗忘、备份迁移五大诉求全部满足。

### 场景四：个人知识管家与 AI 分身助理

**场景描述**：个人用户希望有一个「7×24 小时」的 AI 助理：在工作群里解答问题、沉淀个人知识，离线时还能替你回复。

**典型用法**：
- 桌面版（SQLite + 本地 embedding）单机运行，Windows / macOS / Linux 三端可用，**多实例**（工作窗口 + 家庭窗口共享同一后端，最后一个窗口关闭才停服务）；
- 创建个人知识库（个人文档、笔记、简历、合同模板），绑定给助理角色，之后「帮我基于简历写一份自我介绍」等请求基于你的资料作答；
- 开启 **AI 分身**：根据你在公开群的发言自动生成人设；你在线时自己回复，**离线时分身代班**；在群里 @ 自己还能随时「召唤」分身；
- 启用**个人记忆**，助理越来越了解你的偏好；在**记忆管理**中把重要结论标记为「重要」，让助理长期记住你的关键偏好；
- 用 **话题**区分「工作」「生活」「学习」多线讨论，互不干扰。

**价值**：一个「懂你」的私人助理，随叫随到，替你值守；数据全部在本机，隐私无忧，记忆由你掌控。

### 场景五：教育培训与学术研讨（多导师多学生协同学）

**场景描述**：高校课题组 / 培训班需要多个专业 AI 助教（写作、代码、数学、文献综述）参与群组研讨，并对学生进行个性化辅导。

**典型用法**：
- 一个课程群入驻「写作助教」「代码助教」「数学助教」，分别绑定对应的知识库（教材、讲义、往年试题）；
- 学生发问「@写作助教 帮我校对这段论文摘要，给出修改建议」→ 助教基于知识库与课程要求作答；
- 「@代码助教」在语境触发下主动指出学生代码中的常见错误（全量监听 + 语境判断）；
- 学术讨论按 **话题**分线：「课程答疑」「项目组会」「论文进展」互不打扰；
- 老师开启 **AI 分身**：离线时由分身先回复常见问题，老师上线后接手；
- 把课堂上的关键知识点在记忆管理中标记为「重要」，助教回答时优先引用。

**价值**：多助教并行辅导，减轻教师负担；知识库让答案**对齐课程大纲**而非泛泛而谈；话题组织让研讨有序，记忆分级让重点内容更突出。

### 场景六：跨组织 / 跨区域的多方协作（Hub 级联与外部专家网络）

**场景描述**：两家公司或两个部门共同推进一个项目，各自有内部智能体与知识，需要在不打通全部数据的前提下协同。

**典型用法**：
- A 公司 Hub 的「外部专家」角色以 **hub 方言桥接**到 B 公司 Hub，两套系统级联——A 群里的触发消息转发到 B 的智能体，B 的流式回复回灌 A 的群；
- 外部专家的**审批请求跨 Hub 级联**：B 的智能体要执行敏感操作时，审批卡片出现在 A 的群里，由 A 侧触发者决策；
- 按**话题**隔离多方协作线，每方各管各的群与记忆，**私密群**保证内部讨论不被外部智能体检索；
- 各自按合规要求配置**记忆保留天数**，项目结束可**导出**协作群数据归档，或**初始化**清空外部接入痕迹。

**价值**：让「组织边界」不再阻断 AI 协作；级联审批保证跨组织的操作依然**人可控制**；记忆治理与数据主权各自保留。

---

## 五、部署形态速览

| 形态 | 适用 | 数据存储 | 特点 |
|---|---|---|---|
| **Docker Web**（推荐生产） | 内网服务器 / 云主机 | PostgreSQL（+ pgvector）| 一条 `docker compose up -d` 启动 Web + Postgres + Ollama，RAG 记忆与记忆治理开箱即用 |
| **Windows 桌面版** | 个人 / 小团队单机 | SQLite（+ sqlite-vec）| WPF + WebView2，捆绑本地 embedding 模型，离线可用；**多实例共享后端** |
| **跨平台桌面版** | macOS / Linux / Windows | SQLite | Avalonia 壳，同一套宿主，三端体验一致 |
| **协议 Hub**（仅协议） | 二次开发 / 服务端 | 任意 | 只暴露 AG-UI 协议端点，可嵌入自有系统 |

---

## 六、快速上手（5 分钟）

1. **Docker**：`cp .env.example .env && docker compose up -d --build` → 浏览器打开 `http://localhost:5200`；
2. **桌面版**：安装 MSI（Windows）后双击启动，自动拉起本地服务；
3. 注册 / 登录账号（首次进入按提示配置 DeepSeek endpoint / apiKey，留空即用官方端点与环境变量）；
4. 顶栏「🤖 智能体」创建一个 AI 角色（或从 `tools/agents-starter.json` 一键导入 25 个行业角色）；
5. 左侧「＋」创建群，勾选成员与智能体，@ 它开始对话；
6. 需要外部专家时，在智能体表单填写 AG-UI 桥接端点即可；
7. 聊天一段时间后，到账户菜单 →「记忆管理」体验分级、自动遗忘与可视化治理。

---

## 七、下一步展望

- **更多桥接方言**：接入更多 AG-UI 生态服务与主流编码 / 办公智能体；
- **多模态**：语音 / 图片问答、音视频会议接入；
- **插件市场**：角色包、技能包、知识库模板一键分发；
- **审计与合规**：操作日志导出、角色权限体系（RBAC）增强；
- **记忆增强**：记忆分群分级 / 自动遗忘 / 可视化已落地，下一步支持记忆跨实例同步、重要记忆自动沉淀为知识库文档。
